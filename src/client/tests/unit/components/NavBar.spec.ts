// import { mount } from '@vue/test-utils'
import { describe, expect, it } from 'vitest'

// import NavBar from '@/components/NavBar.vue'

describe('NavBar.vue', () => {
  it('can run a test placeholder', () => {
    expect(true).toBe(true)
  })
  // it('renders properly', () => {
  //   const wrapper = mount(NavBar)
  //   expect(wrapper.exists()).toBe(true)
  // })
  // it('has the correct number of navigation items', () => {
  //   const wrapper = mount(NavBar)
  //   const navItems = wrapper.findAllComponents({ name: 'NavBarItem' })
  //   expect(navItems.length).toBe(3) // assuming 3 items for home, graph, and documentation
  // })
})
